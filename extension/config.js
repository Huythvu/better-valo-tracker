// Deployed Cloudflare Worker URL - no trailing slash.
// After `npx wrangler deploy`, replace this with the URL wrangler prints.
// For local testing against `npx wrangler dev`, use "http://localhost:8787".
self.WORKER_URL = "https://better-valo-tracker.huythvu.workers.dev";
